import 'package:flutter/material.dart';

class OrderDeatiels extends StatelessWidget {
  const OrderDeatiels({super.key});
  static String id = 'OrderDeatiels';

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
